# AmaTradeCore

Это ядро для торгового бота или аналитической платформы на Python.

## Начало работы

1. Склонируйте репозиторий.
2. Скопируйте `.env.example` в `.env` и настройте переменные окружения.
3. Установите зависимости (используя `pip install .` или `poetry install`).
